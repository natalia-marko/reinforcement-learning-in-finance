weekly data - rebalancing monthly (ONCE IN 4 WEEKS), reinforcement learning portfolio system for 7 tech stocks (NVDA, MU, AAPL, AMD, ASML, MSFT, GOOG) using PPO (Proximal Policy Optimization). The system evolves sophisticated neural architectures with attention mechanisms.
Core Purpose: Train an AI agent to dynamically allocate portfolio weights across tech stocks, optimizing for risk-adjusted returns (Sharpe ratio) while managing transaction costs.

System architecture
1.  Data Flow - The Foundation
Raw Price Data (2015-2024) → Weekly Aggregation → 
Train/Test Split (80/20) → Feature Engineering → Normalized Features → RL 
train will be used for train and walk forward validation, test as out of sample data for final testing


### Define Objectives and Constraints

- MY TARGET is to build a reinforcement learning system on weekly data, for rebalansing every 4 weeks portfolio, maximizing sharpe ratio for my personal portfolio
### create n_folds by splitting train data cronologically into train and validation and implement walking forward validation

### Design Environment
START WITH A SIMPLE NN MODEL as a baseline:

class SimpleActor(nn.Module):
    def __init__(self):
        self.net = nn.Sequential(
            nn.Linear(state_dim, 256),
            nn.ReLU(),
            nn.Dropout(0.2),
            nn.Linear(256, 128),
            nn.ReLU(),
            nn.Linear(128, n_assets)
        )
    
    def forward(self, x):
        return F.softmax(self.net(x), dim=-1)
    
... improve reward signal:
# Shorter lookback for denser signal
reward_lookback = 4 
# Or use immediate returns with risk penalty
reward = weekly_return - lambda * portfolio_volatility

add baseline strategy
# Blend RL weights with equal-weight baseline
final_weights = alpha * rl_weights + (1-alpha) * equal_weights
alpha = min(0.7, training_progress)  # Start conservative


AFTER implementing baseline, move to advanced one Complex architecture (Attention + LSTM)
- Create a custom environment class (inheriting from `gym.Env`) with:
  - **State:** Portfolio weights, price history, and financial indicators.
  - **Actions:** Rebalancing allocation decisions (weight changes)[4].
  - **Reward:** Combination of portfolio returns, risk penalties, transaction costs, and turnover[1][3].
- Validate with Toy/Benchmark portfolios before scaling up.

### 4. Specify Weekly Rebalancing Logic

- Implement environment to allow actions only once per week:
  ```python
  if current_week % 4 == 0: # assuming weekly data, rebalance every 4 weeks
      # Allow agent to act (rebalance)
  ```
- Log portfolio adjustment dates and new weights to trace agent's decisions[2].

### 5. Select and Configure RL Model

- Use agent architectures like DQN, PPO, or A2C (from stable-baselines3 or custom):
  - Tune hyperparameters (learning rate, batch size, gamma, etc.).
  - Set up training episodes simulating multiple weeks, running thousands of episodes for policy refinement[1][3][4].

### 6. Train Agent

- Log rewards, actions, and episode summaries for review.
- Save model checkpoints so progress and reproduction are possible.

### 7. Validate & Backtest

- Use out-of-sample (unseen) data to validate agent behavior.
- Compare against benchmarks (equal-weight, buy-and-hold) for Sharpe ratio, drawdown, turnover, etc.[2].
- Document failures/tradeoffs for iterative model improvements.

### 8. Performance Analysis & Visualization

- Visualize portfolio value, asset weights, and signal metrics using matplotlib or Plotly[2].
- Calculate portfolio returns, individual asset returns, allocation weights, and risk metrics within analysis scripts.
- Optionally use libraries like PyFolio for in-depth performance evaluation.

### 9. Documentation & Maintenance

- Store all environment, model, data, and result files in a well-organized project folder.
- Maintain a changelog and markdown documentation (README or mf document).
- Summarize findings, model limitations, and next steps for easy collaboration and review[1].


## RL Environment Design

Document environment specification for repeatable training.

- Implement or customize OpenAI Gym-like interface:
  - `reset()`, `step(action)`, `get_observation()`, etc.
  - Support for multi-asset, action masking, reward normalization.
- Sample structure:
  ```python
  class PortfolioEnv(gym.Env):
      def __init__(self, data, ...):
          # code setup
      def reset(self):
          # reset logic
      def step(self, action):
          # environment update
  ```
- Save environment configs and sample episodes.

## Model Training Guidelines

Outline training, experimentation, and evaluation procedures.

- Baseline: Use DQN or PPO from stable-baselines3.
- Hyperparameter tuning: Learning rate, batch size, reward scaling, episode length.
- Model checkpointing: Save models regularly.
- Experiment logs: Save all metrics, validation scores, and visualizations.

## Evaluation & Backtesting

Steps for model validation and performance review.

- Use out-of-sample backtesting with unseen market periods.
- Metrics: Sharpe ratio, max drawdown, volatility, portfolio turnover.
- Visualize portfolio trajectories:
  ```python
  import matplotlib.pyplot as plt
  plt.plot(portfolio_returns)
  plt.show()
  ```

Critical Design Choice: Monthly rebalancing on weekly data:
    - Transaction costs (2.5 bps per trade)
    - Portfolio Environment (Gymnasium-based):
        - State Space: Asset features + current weights + portfolio stats + time embeddings
        - Action Space: Weight allocation [0,1] for each asset (softmax normalized to sum=1)
        - Reward: Sharpe ratio calculated over 4-week rolling window
        - Constraints: Max 40% per asset, transaction costs, slippage modeling

Neural Architecture (Actor-Critic)
Actor Network (Makes Allocation Decisions):
├── Feature Extraction: 512 → 256 neurons
├── Multi-Head Attention: 8 heads (captures asset correlations)
├── LSTM: 2 layers, 128 hidden (temporal patterns)
└── Softmax Output: Valid portfolio weights

Critic Network (Evaluates State Value):
└── Deep MLP: State → 512 → 256 → 128 → 1 (value)

PPO Training Algorithm

Exploration: Starts at 40%, decays to 5% minimum
Clipped objective: Prevents catastrophic policy updates
Entropy bonus: 0.05 coefficient encourages diversity
Early stopping: Patience=50 episodes on validation Sharpe
